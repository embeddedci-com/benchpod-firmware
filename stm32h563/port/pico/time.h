/* Pico SDK <pico/time.h> shim -> our pico_compat time API.  Lets ported RP2350
   modules (command_handler.c, ...) that include "pico/time.h" build unchanged. */
#ifndef PICO_TIME_SHIM_H
#define PICO_TIME_SHIM_H
#include "pico_compat.h"
#endif
